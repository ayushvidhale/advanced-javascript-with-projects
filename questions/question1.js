let person = {
    name: "Mugemin",
    age: 19,
    country: "Japan"
}

function logData(){
    console.log(person.name + " is " + person.age + " years old and lives in " + person.country)
}

logData()